{
  "name": "ripstaurant",
  "private": true,
  "type": "module",
  "workspaces": ["contract"],
  "scripts": {
    "sample": "bun contract/scripts/build-sample.ts",
    "sample:synthetic": "bun contract/scripts/build-sample.ts --synthetic 10000",
    "test": "bun test",
    "typecheck": "tsc -p contract --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.0"
  }
}
